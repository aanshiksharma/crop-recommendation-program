services:
  backend:
    build: ./backend
    container_name: crop-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: crop-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
